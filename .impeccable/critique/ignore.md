# Impeccable critique — ignored findings

- `overused-font` (Inter) — false positive: detector matches by font name only, doesn't see the deliberate Cinzel/Inter display+body pairing that defines this app's committed visual language ("Command Deck" Ember Red).
